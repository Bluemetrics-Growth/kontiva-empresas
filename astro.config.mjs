import { defineConfig } from "astro/config";

// LP estatica de empresas. Saida em dist/, servida pela Vercel (framework astro).
export default defineConfig({
  site: "https://empresas.kontiva.ai",
});
